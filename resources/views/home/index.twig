{% extends 'app.twig' %}

{% block content %}
    <form action="{{ url_for('home') }}" method="post">
        {{ csrf() | raw }}
        <button type="submit">
            Submit
        </button>
    </form>
{% endblock %}
